# Connexion Redis locale (Docker Desktop/WSL, service en écoute sur 6379)
spring.redis.host=localhost
spring.redis.port=6379
spring.redis.timeout=2000

# Exemple si l'app tourne DANS le même docker-compose que Redis:
# spring.redis.host=redis

# Exemple Redis managé (Upstash/Redis Cloud) en TLS:
# spring.redis.url=rediss://:PASSWORD@HOST:PORT